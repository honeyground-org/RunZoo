/target
/dist
# 참고용 원본 저장소. 우리 저장소에 들고 다닐 필요는 없다.
# 다시 받으려면: git clone --depth 1 https://github.com/runcat-dev/RunCat365 reference/RunCat365
/reference
# bundle.sh 가 gen_icon.py + iconutil 로 매번 다시 만든다
/assets/AppIcon.iconset
/assets/AppIcon.icns
