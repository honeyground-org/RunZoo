/target
/dist
# Upstream repo kept for reference only. No need to carry it in ours.
# To fetch it again: git clone --depth 1 https://github.com/runcat-dev/RunCat365 reference/RunCat365
/reference
# bundle.sh regenerates these every time via gen_icon.py + iconutil
/assets/AppIcon.iconset
/assets/AppIcon.icns
